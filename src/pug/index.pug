doctype html
html
    head
        meta(charset="utf-8")
        title FilterJS
        link(rel="stylesheet" href="/bundle.css")
    body
        root-tag
        script(src="/js/3rdparty/riot.js")
        script(src="/js/index.js")
        script(src="/js/riot.js")
        script.
            window.signals = {};
            riot.observable(window.signals);
            riot.mount('*');